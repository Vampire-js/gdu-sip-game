# Models

Drop `.glb` / `.gltf` files here, then register them in
[`../../src/manifest.ts`](../../src/manifest.ts).

See the top-of-file comment in `manifest.ts` for the naming conventions,
sizing reference (metres), and collider override options.

Files in this folder are served at `/models/<name>.glb` at runtime.
